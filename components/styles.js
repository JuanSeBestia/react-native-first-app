import { StyleSheet } from 'react-native';
export const styles = StyleSheet.create({
    view: {
        backgroundColor: "#CCC",
        minHeight: "100%"
    },
    headerCard: {
        textAlign: "center",
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 16,
    },
    body: {
        marginBottom: 16,
    },
    divider: {
        marginBottom: 24,
    }
});